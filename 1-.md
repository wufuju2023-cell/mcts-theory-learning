下面按历史与数学逻辑，从 bandit → UCB1 → UCT → PUCT 逐步推。核心结论先说：**UCT 的探索项可以从 Hoeffding 不等式严格推出；PUCT 不是 UCT 的同等级严格定理产物，而是把“先验概率 $P(s,a)$”注入 UCT 探索项的启发式构造。AlphaZero 里的 PUCT 形式是历史与工程折中，不是从 Hoeffding 不等式直接唯一推出的。**

---

## 1. 最基础：多臂 bandit 与 UCB1

设有 $K$ 个臂。第 $i$ 个臂第 $t$ 次拉动回报 $X_{i,t}\in[0,1]$，独立同分布，均值

$$
\mu_i=\mathbb{E}[X_{i,t}].
$$

拉动 $n_i$ 次后经验均值

$$
\hat\mu_i=\frac{1}{n_i}\sum_{t=1}^{n_i}X_{i,t}.
$$

Hoeffding 不等式给出：

$$
\Pr(\hat\mu_i\ge \mu_i+\epsilon)\le e^{-2n_i\epsilon^2},
\quad
\Pr(\mu_i\ge \hat\mu_i+\epsilon)\le e^{-2n_i\epsilon^2}.
$$

所以以高概率：

$$
|\hat\mu_i-\mu_i|\le \sqrt{\frac{\ln(1/\delta)}{2n_i}}.
$$

取总轮数 $n$，令 $\delta\sim n^{-2}$，则

$$
\mu_i\le \hat\mu_i+\sqrt{\frac{2\ln n}{n_i}}.
$$

于是自然选择“乐观上界”最大的臂：

$$
a_t=\arg\max_i\left[
\hat\mu_i+\sqrt{\frac{2\ln n}{n_i}}
\right].
$$

这就是 UCB1。它的严格 regret 是 $O(\log n)$。关键结构是：

$$
\text{经验价值}+\text{不确定性惩罚}.
$$

不确定性惩罚随总轮数 $n$ 缓慢增长，随该臂访问次数 $n_i$ 下降。

---

## 2. 从 bandit 到 MCTS：UCT

MCTS 中，每个树节点 $s$ 可以看成一个 bandit：可选动作是子节点 $a$。定义：

- $N(s,a)$：从状态 $s$ 选动作 $a$ 的次数；
- $N(s)=\sum_b N(s,b)$：状态 $s$ 被访问总次数；
- $Q(s,a)$：动作 $a$ 的平均回报估计。

把 UCB1 直接套到节点 $s$：

$$
a=\arg\max_a\left[
Q(s,a)+c\sqrt{\frac{\ln N(s)}{N(s,a)}}
\right].
$$

这就是 **UCT**，由 Kocsis 和 Szepesvári 在 2006 年提出。历史地位是：它把 bandit 的置信上界思想搬到了树搜索中，使 MCTS 有了理论上的收敛依据。

但 UCT 的探索项只依赖访问次数，不依赖任何先验。围棋分支因子极大，均匀探索非常浪费。于是需要先验。

---

## 3. PUCT 的历史出场

PUCT 来自 Rosin 2011 年的工作，全称可理解为 **Predictor + UCT**。它假设有一个预测器给出每个动作在当前状态下“可能最优”的先验概率 $P(s,a)$。AlphaGo 2016 采用它，AlphaGo Zero / AlphaZero 进一步使用带 $\sqrt{\sum_b N(s,b)}$ 的形式。

AlphaZero 中最终选择公式是：

$$
a_t=\arg\max_a\left[
Q(s,a)+c_{\text{puct}}P(s,a)
\frac{\sqrt{\sum_b N(s,b)}}{1+N(s,a)}
\right].
$$

其中

$$
U(s,a)=c_{\text{puct}}P(s,a)
\frac{\sqrt{\sum_b N(s,b)}}{1+N(s,a)}.
$$

AlphaGo 2016 的版本常写作：

$$
U(s,a)=c_{\text{puct}}\frac{P(s,a)}{1+N(s,a)}.
$$

AlphaGo Zero / AlphaZero 又加上了 $\sqrt{\sum_b N(s,b)}$。

---

## 4. PUCT 公式的构造性推导

如果严格从 Hoeffding 推，先验加权 UCB 最自然的形式应是：

$$
Q(s,a)+cP(s,a)\sqrt{\frac{\ln N(s)}{N(s,a)}}.
$$

也就是说：先验越大，探索项越大；访问越多，探索项越小；总访问越多，探索项缓慢增长。

但 AlphaZero 实际用的是：

$$
Q(s,a)+cP(s,a)\frac{\sqrt{N(s)}}{1+N(s,a)}.
$$

它做了三个替换：

1. **分母 $N(s,a)$ 换成 $1+N(s,a)$**
   避免 $N(s,a)=0$ 时除零，同时使初始探索项有限，且正比于先验 $P(s,a)$。
2. **$\sqrt{\ln N(s)}$ 换成 $\sqrt{N(s)}$**
   让总访问增长时探索温度增长更快，防止过早利用。严格 UCB 用 $\ln N$，PUCT 用 $\sqrt{N}$，这不是 Hoeffding 的直接结果，而是启发式替换。
3. **乘以先验 $P(s,a)$**
   让先验高的动作在早期优先被探索。

更关键的是，核心项

$$
\frac{P(s,a)}{1+N(s,a)}
$$

有一个很好的数学解释：如果暂时忽略 $Q(s,a)$，只看探索项，那么选择

$$
a=\arg\max_a \frac{P(s,a)}{1+N(s,a)}.
$$

当某个动作被访问太多，分母变大，它会被降权；当某个动作先验高但访问少，它会被优先。稳定后，访问次数大致满足

$$
N(s,a)\propto P(s,a).
$$

也就是说，PUCT 的探索项设计目标之一是：**让访问次数按先验概率比例分配**。这正好符合策略网络输出 $P(s,a)$ 的语义。

而前面的 $\sqrt{N(s)}$ 对所有动作相同，不改变纯探索时的排序，但它决定探索项相对于 $Q(s,a)$ 的整体尺度，起到“探索温度调度”的作用。

---

## 5. 严格性边界

必须强调：

- UCB1 有严格 Hoeffding 推导和 $O(\log n)$ regret。
- UCT 有在 minimax 树等假设下的收敛性证明。
- PUCT 没有 UCB1 那样干净、统一的严格 regret 界。它更像：

$$
\text{UCT 的访问惩罚}
+
\text{策略先验加权}
+
\text{全局探索温度}.
$$

如果强行从第一性原理推，先验加权 UCB 更自然是

$$
Q+cP\sqrt{\frac{\ln N}{N_a}},
$$

而不是

$$
Q+cP\frac{\sqrt{N}}{1+N_a}.
$$

AlphaZero 的形式是历史与工程选择的产物：它在围棋等巨大动作空间中经验上非常有效，但不能说它是从某个不等式唯一严格导出的。

---

## 6. 一句话总结公式来源

历史链条是：

$$
\text{UCB1}
\Rightarrow
\text{UCT}
\Rightarrow
\text{PUCT}.
$$

数学上：

$$
\text{乐观上界}=\text{经验价值}+\text{不确定性}.
$$

MCTS 中变成：

$$
Q(s,a)+c\sqrt{\frac{\ln N(s)}{N(s,a)}}.
$$

加入先验后，PUCT 把它改造成：

$$
Q(s,a)+c_{\text{puct}}P(s,a)
\frac{\sqrt{\sum_b N(s,b)}}{1+N(s,a)}.
$$

所以，PUCT 的“推导”不是严格定理推导，而是：

> 把 UCB 的置信上界思想、MCTS 的访问计数、策略先验 $P(s,a)$、以及比例探索的设计目标，融合成一个启发式选择规则。

最终选择：

$$
\boxed{
a_t=\arg\max_a\left[
Q(s,a)+c_{\text{puct}}P(s,a)
\frac{\sqrt{\sum_b N(s,b)}}{1+N(s,a)}
\right]
}
$$

这就是 AlphaZero 中使用的 PUCT 公式。
