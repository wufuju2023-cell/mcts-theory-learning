# 主 RL 与 TTRL 的区别，以及“大模型基座 + 价值头”的复现分析

参考：T. Hubert et al., "Olympiad-level formal mathematical reasoning with reinforcement learning", Nature (2025)。本文承接第 5、6 篇的记号：先把 AlphaProof 的主 RL 与 TTRL 放在同一个数学框架里逐项对照，再分析一条开源复现路线——**策略网络直接取一个大模型作为基座，在它上面训练价值头**。

---

## 0. 共同框架与记号

**证明 MDP.** 状态 $s=(\Gamma,\Delta)$ 是规范化后的 Lean 战术状态（$\Gamma$ 为假设上下文，$\Delta$ 为开放目标），动作 $a$ 是一条战术字符串，转移确定性 $s'=\mathrm{exec}(s,a)$ 或无效。奖励恒为

$$
r_t=-1,
$$

回报与价值

$$
G_t=\sum_{k\ge t}r_k,\qquad
V(s)=\mathbb{E}[G_t\mid s_t=s]=-d(s),
$$

其中 $d(s)$ 是从 $s$ 出发解决全部开放目标的期望步数。拆分目标的 AND 节点取最小回报：

$$
G(s_{\mathrm{AND}})=\min_j G\big(s^{(j)}\big)
\iff
d(s_{\mathrm{AND}})=\max_j d\big(s^{(j)}\big).
$$

**搜索（Sampled-AlphaZero 证明版）.** 选择公式为

$$
a^\star=\arg\max_a\left[
Q(s,a)+c(s)\,\pi^{1/\tau}(a\mid s)\,
\frac{\sqrt{N(s)}}{N(s,a)+1}
\right],
\qquad
Q(s,a)=\gamma^{-V(s,a)-1},
$$

其中 $c(s)=c_{\text{init}}+\log\frac{N(s)+c_{\text{base}}+1}{c_{\text{base}}}$；AND 节点把 $Q$ 换成 $1-Q$，使搜索优先攻击最难子目标；满足 $n(s)\le CN(s)^{\alpha}$ 时做渐进采样；一次尝试只维护一棵树，直到 `proved`、`disproved` 或预算耗尽。

**训练信号.** 只使用已验证的成功轨迹：策略项为交叉熵

$$
\mathcal{L}_{\text{pol}}(\theta)=-\log\pi_\theta(a\mid s),
$$

其中 $a$ 是成功路径上实际应用的战术；价值项为分箱交叉熵

$$
\mathcal{L}_{\text{val}}(\theta)=-\log p_v\big(\mathrm{bin}(G(s))\mid s\big),
\qquad
\widehat V(s)=-\sum_b m_b\,p_v(b\mid s).
$$

自生成数据与 Mathlib SFT 数据的混合比约为 $9:1$（SFT 占 $10\%$）。

**Matchmaker.** 有趣度优先级

$$
I(p)=\text{high}
\iff
\Big[n_p=0\ \lor\ n_p<\mathrm{TC}\ \lor\
\big(n_p\ge\mathrm{TC}\ \land\ 0<s_p<1\big)\Big],
$$

自适应预算

$$
B(p)=\min\Big(B_{\text{cap}},\ B_0\,m^{\,f_p}\Big),
$$

其中 $s_p$ 是最近 $N$ 次尝试中被解决的比例，$f_p$ 是其中的超时次数。重建参考值：$\mathrm{TC}=5$，$N=25$，$B_0=500$，$m=2$，$B_{\text{cap}}=15000$。

---

## 1. 主 RL 与 TTRL：同一个算法，不同起始测度

### 1.1 共同内核

两者共用完全相同的六个部分：

- **环境**：Lean 战术 MDP、AND 拆分、验证与内核检查；
- **搜索**：PUCT 选择、$Q$ 变换、AND 节点规则、渐进采样、单树搜索；
- **调度**：matchmaker 的有趣度与自适应预算公式；
- **经验生成**：actors 只回传已验证的证明/证伪轨迹，失败与超时被过滤；
- **学习器**：成功轨迹上的策略交叉熵 + 分类价值交叉熵，$90/10$ 混合；
- **价值语义**：$G=-(T-t)$（AND 节点取 min），蒙特卡洛回报，不做 bootstrap。

因此 TTRL 不是另一套算法，而是**把同一算法作用在另一个起始分布与停止规则上**。下面把差异逐项形式化。

### 1.2 差异的逐项展开

**(1) 目标函数不同.**

主 RL 在整个问题分布上做期望风险最小化（每次尝试随机指派“证明”或“证伪”目标）：

$$
\max_\theta\ J_{\text{main}}(\theta)
=\mathbb{E}_{p\sim\mathcal{D}_{\text{start}}}
\Big[\Pr_{\theta}\big(\text{在预算 }B(p)\text{ 内解决 }p\big)\Big],
$$

$$
\mathcal{D}_{\text{start}}=\mathcal{D}_{\text{AutoForm}}\cup\mathcal{D}_{\text{human}},
\qquad
|\mathcal{D}_{\text{AutoForm}}|\approx8\times10^{7},
\qquad
|\mathcal{D}_{\text{human}}|\approx3.5\times10^{3}.
$$

TTRL 固定目标 $T$（或一组 $\{T_j\}$），只关心单点：

$$
\max_\theta\ J_T(\theta)=\Pr_{\theta}(\text{证明 }T),
$$

实际优化的是课程代理

$$
\max_\theta\ \sum_{v\in V_T} w_T(v)\,\Pr_{\theta}(\text{证明 }v),
$$

其中 $V_T$ 是 $T$ 的合成变体集，权重 $w_T(v)$ 由 matchmaker 的有趣度/预算动态实现，等价于一个以 $T$ 为中心的加权课程测度。

**(2) 初始化不同.**

主 RL 从预训练 + SFT 的检查点出发；TTRL 从主 RL 得到的通用模型 $\theta^\ast_{\text{mainRL}}$ 出发，再做问题定向的 RL。因此 TTRL 也可以理解为“带课程生成的 RL 微调”。

**(3) 课程来源不同.**

主 RL 的课程是收集来的：自动形式化把约 $10^6$ 条自然语言命题变成约 $8\times10^7$ 条 Lean 命题，加上约 $3.5\times10^3$ 条人工形式化命题。TTRL 的课程是为 $T$ 现场生成的：

- 生成器 $\mathcal{G}$：LLM 少样本提示，示例集约 $791$ 对，覆盖 Pólya 式启发式——简化、泛化、引理、类比、改写、证明步骤、分解；
- 程序化算子集 $\Sigma$：数值扰动、加强假设、$\exists$ 与 $\forall$ 互换、子集与等式互换、参数限制等，保证局部变体的系统性覆盖；
- 所有候选必须通过 Lean 语法与类型检查（合法开命题），再规范化去重；
- 进化扩展：

$$
V_0=\mathrm{Valid}\big(\mathcal{G}(\Omega,T)\big),
\qquad
V_{i+1}=V_i\cup\mathrm{Valid}\Big(\mathcal{G}\big(\mathrm{top}_k\ V_i\ \text{按}\ \mathrm{sim}(T,\cdot)\big)\Big),
\quad i<N_{\text{evo}},
$$

论文取 $N_{\text{evo}}=15$，最终 $|V_T|\approx10^{5}$ 到 $10^{6}$ 条唯一合法变体。

**(4) 终止规则不同.**

主 RL 按固定日程训练（约 $10^6$ 次参数更新）；TTRL 一旦 $T$ 被证明，matchmaker 就停止分配 $\{T\}\cup V_T$——目标达成即停。多目标可共享一个 matchmaker 并行推进。

**(5) 预算与规模不同.**

主 RL 约 $8\times10^4$ TPU-days。TTRL 每个目标族约 $50$ 到 $500$ TPU-days（IMO 2024 单题在其规模下约 $2$ 到 $3$ 天）；变体生成相对便宜（每目标约 $100$ 到 $500$ GPU-hours）。

**(6) 度量不同.**

主 RL 观测 held-out 基准的 solve rate 对模拟预算的曲线（训练曲线按 TPU-days 记录 proved / disproved / undecided）；TTRL 直接看目标 $T$ 是否被证明，并在基准上度量增益。论文测量：在 500 TPU-days 的 TTRL 下，formal-imo 与 PutnamBench-test 上相对纯搜索约 $+15$ 个百分点。

**(7) 防遗忘要求不同.**

主 RL 本身就是通用训练，不存在遗忘问题；TTRL 是定向微调，长期运行会侵蚀通用能力，需要护栏（兄弟题 held-out 监控、连续两次退化即停、SFT 混合重新采样）。

### 1.3 为什么 TTRL 有效：理论视角

**同一改进算子，不同测度.** 两者都在执行专家迭代：搜索定义改进策略 $\pi_B$，训练把 $\pi_\theta$ 拉向成功轨迹的分布。差别只在数据分布的测度：主 RL 的测度是 $\mathcal{D}_{\text{start}}$ 上的均匀（按有趣度重加权），TTRL 的测度集中在 $T$ 的邻域。用性能差引理看，每次成功搜索带来的改进量为

$$
J(\pi')-J(\pi)=\frac{1}{1-\gamma}\,\mathbb{E}_{s\sim d^{\pi'}}\Big[\sum_a\pi'(a\mid s)A^{\pi}(s,a)\Big],
$$

TTRL 把这条式子里的期望测度换成了 $\{T\}\cup V_T$ 诱导的测度，因此梯度信号集中落在与 $T$ 相关的状态区域。

**共享骨架.** 每个变体 $v$ 与 $T$ 共享数学骨架（引理、界、不变量），所以成功变体产生的参数更新直接改善 $T$ 附近的策略与价值；变体越接近 $T$，单位算力对 $T$ 的信息量越大。

**难度存在最优区间.** 消融显示：更难但可解的变体教学效果更好（生成器更强时变体本身解得更少，但教出来的目标解决率更高）。直观解释是信息增益：太简单的变体几乎不改变策略分布；太难的变体不在当前策略的可达支撑内，无法产生成功轨迹。最优课程位于“可解边缘”。

**min 回报的作用.** AND 节点的 min 回报让搜索预算自动集中在最难分支——而这些分支通常正是 $T$ 的关键障碍，因此 TTRL 的搜索与价值都天然对准 $T$ 的瓶颈。

### 1.4 一句话对比

- **目标**：主 RL 是分布上的期望解决率；TTRL 是单点 $\Pr(\text{证明 }T)$；
- **起始分布**：主 RL 约 8000 万条自动形式化命题；TTRL 是 $\{T\}$ 加其数十万条变体；
- **初始化**：主 RL 从预训练 + SFT 出发；TTRL 从主 RL 通用模型出发；
- **课程构造**：主 RL 用已有数据集；TTRL 现场生成 + 进化 + 验证；
- **终止**：主 RL 固定日程；TTRL 解出即停；
- **规模**：主 RL 约 $8\times10^4$ TPU-days；TTRL 每目标族 $50$ 到 $500$ TPU-days；
- **算法本身**：完全相同——同一环境、搜索、损失、matchmaker。

---

## 2. 大模型基座 + 价值头：开源复现的分工分析

### 2.0 设定与两种配置

设基座模型参数为 $\theta_0$，策略为在基座上做低秩适配（或全参微调）的自回归分布

$$
\pi_\theta(a\mid s)=\prod_j p_{\theta_0+\Delta\theta}\big(a_j\mid a_{<j},\ \mathrm{tok}(\mathrm{pp}(s))\big),
$$

其中 $\Delta\theta$ 是 LoRA 参数 $W=W_0+BA$；价值头 $v_\psi$ 读取同一模型的隐状态：

$$
v_\psi(s)=\sum_b m_b\,p_\psi\big(b\mid h_\theta(s)\big),
\qquad
h_\theta(s)=\text{末 token 或专用 VALUE token 的隐状态}.
$$

两种配置的结论不同，先分开：

- **变体 A：基座冻结，只训价值头。** 策略固定为 $\pi_0$，RL 只能改善**搜索**，不能改善**策略**；
- **变体 B：基座 + LoRA/全参 + 价值头，全部进入 RL 闭环。** 这是完整的专家迭代，也是下面 (a)(b)(c) 的默认对象。

### 2.1 （a）可以不再做的

- **证明网络的从零预训练**：原文用约 300B token（编码器侧处理约 12T token，解码器侧约 3T）建立代码/数学/逻辑先验。基座模型已经提供了这些先验，这段可以整体省掉。省掉的是“造基座”，不是“用基座”。
- **编码器-解码器架构与 3B 参数工程**：decoder-only LLM 加一个价值头即可替代。共享躯干天然满足“策略与价值共享表示”，而且价值读取可以复用策略前向的隐状态，边际成本接近零。
- **分词器与词表设计**：直接用基座分词器。只有当 Lean 符号分词效率明显差时才考虑扩词表——那反而是**新增**工作（新 embedding 初始化 + 继续预训练 + 分布对齐）。
- **自动形式化模型本身的训练**：原文用微调 Gemini + STaR 生成约 8000 万条课程，投入约 $10^5$ TPU-days。复现时可以换用公开语料（Mathlib、LeanWorkbook、公开 autoformalized 数据集）或外部 API。注意课程规模是性能变量，砍得太狠会损失泛化。
- **Lean 侧重工程**：tactic 编译加速、心跳限制、整数上限、状态序列化等，社区栈（LeanDojo、REPL、Mathlib 缓存）大多已经提供，不值得重造。
- **变体 A 额外可省**：策略侧的一切训练——SFT 策略头、RL 中的策略交叉熵、TTRL 的策略更新都不再适用。

需要提醒的是：**“省掉预训练”不等于“省掉领域适配”**。如果基座不是 Lean 方向的模型，直接用零样本提示采样战术的合法率会很低，搜索效率极差；这时 SFT/继续训练仍然在必做清单里，只是规模远小于从零预训练。

### 2.2 （b）仍然必须做的

- **Lean 环境与验证闭环**：状态/动作/奖励定义、AND 拆分、超时、终局判定（proved/disproved/timeout）、内核检查与公理白名单。若复刻反证机制，还要实现取反算子和相应公理簿记。
- **状态规范化**：$\mathrm{pp}(s)$、假设排序、$\alpha$-重命名、语法去重。这不是工程细节，而是价值头良定义的前提（见 2.3.5）。
- **搜索**：PUCT 选择、$c(s)$、$Q$ 变换、AND 节点 $1-Q$、渐进采样、代价合并、回传（OR 取 min、AND 取 max）、单树不承诺。
- **价值头训练**：分类交叉熵、log 间隔分箱、目标 $G(s)=-(T-t)$、AND 节点取 $\min_j G(s^{(j)})$、蒙特卡洛回报（不做 bootstrap）。
- **专家迭代闭环**：matchmaker（有趣度、自适应预算）、actors（搜索生成已验证轨迹）、learner（策略交叉熵 + 价值交叉熵 + $90/10$ 混合）、回放窗口与 staleness 控制。
- **轻量 SFT/领域适配**：用 Mathlib 的（状态，战术）对对基座做 LoRA 或继续训练，使其成为合格的战术生成器。若基座本身已是 Lean prover，这一步可以很轻，但不能完全跳过（至少要校准输出格式与语法成功率）。
- **评测与数据卫生**：预算对齐的基准评测、bootstrap 置信区间、训练课程与评测集去污染（包括近似变体）。
- **（追高分需要）TTRL 全套**：变体生成、Lean 验证、进化扩展、定向 RL、停止规则、防遗忘护栏。
- **变体 A 中仍然要做**：环境、搜索、价值头训练（此时目标是固定的 $V^{\pi_0}$，是一个静态回归问题）、评测；策略侧训练可省，但性能上限受制于 $\pi_0$（见 2.3.9）。

### 2.3 （c）额外需要做的：大模型基座特有的问题

#### 2.3.1 价值头的参数化、语义与初始化

价值头的输出必须是“剩余步数”的估计，而不是胜率：

$$
\widehat V_\psi(s)=-\sum_b m_b\,p_\psi\big(b\mid h_\theta(s)\big),
$$

$m_b$ 是分箱中点，分箱建议按对数间隔覆盖 $[0,D_{\max}]$（与 $Q$ 变换的精度分布匹配：离目标近处更细）。训练损失按**搜索分布**加权：

$$
\mathcal{L}_{\text{val}}(\theta,\psi)
=\mathbb{E}_{s\sim d^{\text{search}}}
\Big[-\log p_\psi\big(\mathrm{bin}(G(s))\mid h_\theta(s)\big)\Big].
$$

初始化有小技巧：从 Mathlib 证明链里免费获得标签。对长度为 $T$ 的成功链 $s_0\to\dots\to s_T$，令

$$
G(s_t)=-(T-t),\qquad t=0,\dots,T,
$$

AND 节点取所有分支的最小值；超过 $D_{\max}$ 的截断。用这批标签先做一次监督回归（等价于原论文 SFT 阶段的价值头初始化），可以显著缩短 RL 早期搜索的“价值噪声”阶段。

如果价值头改用标量回归而不是分类，最小化 $(\widehat V-G)^2$ 的解同样是条件期望，但分类头校准更稳、对离群回报更鲁棒，且天然给出了价值的不确定性（分布宽度），便于后续做风险控制。

#### 2.3.2 价值误差如何进入搜索：定量界

价值头不必完美，但必须“足够准”。可以把搜索在贪婪极限下的证明长度损失界定如下。

**命题.** 设 $|\hat d(s)-d^*(s)|\le\varepsilon<1/2$ 对所有状态成立，$\pi$ 是对 $\hat d$ 的贪婪策略（每步取 $\arg\min_a\hat d(s'(a))$）。则从 $s_0$ 出发，$\pi$ 产生的证明长度满足

$$
d^\pi(s_0)\le\frac{d^*(s_0)}{1-2\varepsilon}.
$$

**证明.** 记 $a^*=\arg\min_a d^*(s'(a))$，$\pi(s)=\arg\min_a\hat d(s'(a))$。由 $\varepsilon$-精度与贪婪性：

$$
d^*\big(s'(\pi(s))\big)
\le \hat d\big(s'(\pi(s))\big)+\varepsilon
\le \hat d\big(s'(a^*)\big)+\varepsilon
\le d^*(s)-1+2\varepsilon.
$$

即势函数 $\Phi=d^*$ 沿 $\pi$ 的轨迹每步至少下降 $1-2\varepsilon$；望远镜求和并把 $\Phi\ge0$ 代入，得 $T\le d^*(s_0)/(1-2\varepsilon)$。∎

**推论（误差预算的含义）.**

- $\varepsilon=0.1$ 步时最坏情况证明长 $25\%$；$\varepsilon=0.25$ 步时最坏长一倍。要控制长度损失在 $10\%$ 以内，需要 $\varepsilon\lesssim0.045$ 步；
- 更实用的表述：只有当两个动作的真实价值差大于 $2\varepsilon$ 时，价值头才“必须”分辨它们。搜索的实际判决只依赖价值差与 $\varepsilon$ 的相对大小；
- 在一般折扣 MDP 里，对应的结论是 $\varepsilon$-准确价值下的贪婪策略满足 $\|V^\pi-V^*\|_\infty\le 2\varepsilon/(1-\gamma)$（第 6 篇 3.3 节），形式相同：误差被 $1/(1-\gamma)$ 放大。

因此复现时要建立“价值校准到搜索质量”的度量链：分箱交叉熵与 ECE 只是中间指标，最终指标是相同模拟预算下的 solve rate。

#### 2.3.3 Q 变换的误差阻尼

搜索里的价值不直接参与选择，而是先经过 $Q=\gamma^{-V-1}$。设步数误差为 $k$ 步：

$$
\frac{\hat Q}{Q}
=\gamma^{\hat d-d}
=\gamma^{k}.
$$

由于 $\gamma\in(0,1)$，无论误差多大，Q 的相对误差总有界（$\gamma^k\in(0,1)$，相对偏差不超过 $1$）；而原始步数表示下，误差 $k$ 相对于深度 $d$ 的比值 $k/d$ 是无界的。这带来两个工程结论：

- **远端价值误差的破坏是乘性的**：$\gamma=0.99$ 时 $10$ 步误差只造成 $10\%$ 的 Q 偏差，且不随深度放大；
- **价值头只需要相对准确**：近目标的绝对精度要求高（每步 $1\%$），远端的精度要求自然放松——这与对数分箱的容量分配一致。

#### 2.3.4 两时间尺度训练

基座加 LoRA 时，隐状态 $h_\theta$ 随 $\theta$ 移动，价值头的输入分布非平稳；同时价值目标 $V^{\pi_k}$ 随策略改进不断漂移（策略越好，剩余步数越小）。把两者的更新写成

$$
\theta_{k+1}=\theta_k-\alpha_k\nabla_\theta\mathcal{L}_{\text{pol}},
\qquad
\psi_{k+1}=\psi_k-\beta_k\nabla_\psi\mathcal{L}_{\text{val}},
$$

则这是典型的双时间尺度随机逼近，需要人为建立阶差：

- **价值快于策略**（推荐）：价值头小、学习率更大，或每个策略更新周期内把价值头多训若干步；否则搜索用的是过期价值，等价于给 2.3.2 引入一个随迭代增长的评价误差 $\epsilon_k$；
- **显式交替**：冻结策略，把价值重拟合到收敛，再放开策略（接近“拟合价值迭代”）。工程上最容易调试；
- **避免 bootstrap 偏差**：AlphaProof 的价值目标是搜索得到的 MC 回报（$\lambda=1$），从源头避免了自举偏差；如果为了样本效率改用 TD 目标，应加目标网络或 EMA，否则价值误差会通过自举被放大。

监控指标：价值损失的指数移动平均、$\widehat V$ 与实测回报的相关性、以及搜索中“未访问边初始化误差”（父节点预测减惩罚）。

#### 2.3.5 表示良定义性与长状态处理

价值目标 $\mathbb{E}[G\mid s]$ 只对“同一个逻辑状态”才有定义。若同一逻辑状态存在多种 token 表示（假设顺序不同、匿名变量名不同、格式噪声），则

$$
\mathbb{E}\big[G\mid\mathrm{tok}(s)\big]\neq\mathbb{E}\big[G\mid\mathrm{tok}(s')\big],
\qquad s\equiv s',
$$

价值头学到的就是多值函数的噪声。因此规范化必须在环境层强制：确定性 pretty-print、假设排序、$\alpha$-重命名、语法去重，并保证训练与搜索使用同一套规范化。

长状态要单独设计：Mathlib 里的战术状态可能超出基座上下文窗口。需要定义截断/分块/摘要算子，并要求它对策略与价值**一致**且是良定义的（截断本身可以损失信息，但要保证同一状态在训练与搜索时得到相同处理，否则引入系统性偏差）。

#### 2.3.6 分布偏移、staleness 与信任域

价值头只需在**搜索访问到的状态分布**上准确，而不是在 Mathlib 状态分布上准确；因此损失应按搜索分布加权，且回放缓冲区必须保持足够新鲜，控制行为策略与当前策略的差距（staleness）。形式上可以把策略更新写成 KL 正则化目标：

$$
\max_\theta\ \mathbb{E}_{s\sim d^{\text{search}}}
\Big[\sum_a\pi_\theta(a\mid s)\,A(s,a)\Big]
-\beta\,\mathrm{KL}\big(\pi_\theta\,\|\,\pi_{\text{ref}}\big),
$$

其解形如 $\pi_\theta(a\mid s)\propto\pi_{\text{ref}}(a\mid s)\exp\big(A(s,a)/\beta\big)$，即“优势加权 + 参考锚定”。工程上有三件事可以承担 $\pi_{\text{ref}}$ 的角色：$10\%$ 的 SFT 混合、LoRA 的低秩容量（限制每轮策略移动的距离）、以及显式的参考模型 KL。

#### 2.3.7 LLM 化的搜索成本模型

把一次模拟拆开：选择阶段读取已有统计（便宜）；扩展阶段对叶状态做一次前向（KV 缓存可在同一状态的多次扩展间复用），再采样 $K$ 条战术并逐条验证；回传更新统计。单次模拟成本约为

$$
C_{\text{sim}}\approx c_1\,K\,L_{\text{tac}}\,d^2+c_2\,t_{\text{exec}},
$$

其中 $L_{\text{tac}}$ 是战术 token 数，$t_{\text{exec}}$ 是 Lean 执行与验证时间，状态前向 $O(L_{\text{state}}d^2)$ 被同一节点的多次扩展摊销。全流程成本为“问题数 × 预算 $B(p)$ × $C_{\text{sim}}$”，与原论文一致：**搜索算力主导，学习器算力次要**（学习器单步约 $6N_\theta\cdot\text{batchTokens}$）。

大模型基座带来的三个额外工程点：

- **KV 缓存与批处理**是能否跟上搜索吞吐的关键；$K$ 条采样如果逐条串行，成本线性膨胀；
- **价值读取几乎免费**（复用同一次前向的隐状态过一个小 MLP），这是相对原论文编码器-解码器架构的红利；
- **量化会改变隐状态**，进而改变价值头输出；做 QLoRA 时需要重新校准价值（量化误差可能超过 2.3.2 的误差预算）。

#### 2.3.8 数据与标签工程

- **Mathlib 抽取**：从人类证明链得到（状态，战术）对，同时免费得到剩余步数标签 $G(s_t)=-(T-t)$（AND 节点取 min，超 $D_{\max}$ 截断）；
- **价值偏置**：专家迭代只用成功轨迹，失败与超时被丢弃，因此困难区域没有直接价值监督，只有 AND min 与搜索路径的间接覆盖。开源复现可以加一个辅助二分类头（“该状态是否有希望在预算内解决”），给困难状态提供额外信号；
- **TTRL 基础设施**：需要一个会 Lean 的变体生成模型 + 验证/去重/进化管线，以及对课程难度与规模的调度；
- **去污染**：评测题及其近似变体绝不能进入主 RL 课程或 TTRL 课程。

#### 2.3.9 只训价值头（变体 A）的特殊结论

若基座冻结、只训价值头，那么价值目标固定为 $V^{\pi_0}$，是一个**静态**回归问题（没有 2.3.4 的非平稳问题），价值头可以放心训到收敛。但它带来的收益有明确上限：

- 策略分布永远不变，专家迭代的策略改进项为零；
- 搜索质量提升只来自评价变准（2.3.2 的误差项下降），以及预算重分配更合理；
- 形式化地，解决概率的上界是“$\pi_0$ 的支撑内、在搜索预算内被找到的证明”的测度——价值头只能让这个上界更容易被逼近，不能抬高它。

因此变体 A 适合作为复现的第一个里程碑（验证环境、搜索、价值校准），但完整复现需要变体 B：只有策略进入闭环，改进才能持续。

### 2.4 最小可行栈与里程碑

按依赖顺序排列，前四步是地基，后三步是收益与评测：

1. **Lean 环境封装**：战术执行、验证、内核检查、公理白名单、状态规范化；
2. **基座战术生成**：LoRA/继续训练 + 语法合法率评测（先做零样本探测，再决定 SFT 规模）；
3. **价值头**：分类头、log 分箱、Mathlib 步数标签初始化、搜索分布上的校准；
4. **PUCT 搜索**：OR/AND 节点、$Q$ 变换、渐进采样、单树搜索、代价合并；
5. **专家迭代闭环**：matchmaker + actors + learner，$90/10$ 混合，MC 价值目标，staleness 控制；
6. **评测体系**：预算对齐、bootstrap 置信区间、价值校准到搜索质量的度量链；
7. **可选进阶：TTRL**：变体生成、进化扩展、定向 RL、停止规则与防遗忘护栏。

---

## 3. 结论

- **主 RL 与 TTRL 的差别不在算法**：环境、搜索、损失、调度完全相同；差别在目标函数（分布期望对单点概率）、起始分布（收集的课程对生成的课程）、初始化（SFT 对通用模型）、终止规则（固定日程对解出即停）与算力规模（$8\times10^4$ TPU-days 对每目标 $50$ 到 $500$ TPU-days）。理论上可以把 TTRL 看作“测试时把 RL 重新指向 $T$ 邻域”的分布适应。
- **用大模型基座的复现分工**：可以省掉的是“造模型”（预训练、架构、分词器、自动形式化模型训练、Lean 重工程）；不能省的是“造验证器 + 搜索 + 迭代闭环”；必须新做的是“把价值头正确装到大模型上”——包括价值语义与分箱、误差预算（2.3.2 的 $\varepsilon$ 界）、Q 变换的误差阻尼、两时间尺度训练、规范化良定义、分布偏移与信任域、搜索成本模型与量化校准。
- **一句话**：策略更新决定性能的上界，价值精度决定逼近上界的速度；只训价值头能改善搜索但不能抬高上界，只有把策略放进 RL 闭环，专家迭代的改进项才能持续为正。

