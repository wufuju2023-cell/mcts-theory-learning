`6-alpha-proof-mcts-rl.md:214` 这句是尺度分析（motivation），把它写全的推导如下。

**1. 先把“谁支配谁”写成不等式。** 索引是

$$
I(s,a)=\underbrace{Q(s,a)}_{\text{价值项}}+\underbrace{c(s)\,\pi^{1/\tau}(a\mid s)\frac{\sqrt{N(s)}}{N(s,a)+1}}_{\text{探索项 }B(s,a)}.
$$

同一节点上比较两个动作，探索发生（拉一条“看起来更差”的臂）的条件是探索项差压过价值差：

$$
\underbrace{c\,\pi^{1/\tau}\sqrt{N}\Big(\frac{1}{N_2+1}-\frac{1}{N_1+1}\Big)}_{\Delta B}
\;>\;
\underbrace{d_2-d_1}_{\text{步差}}.
$$

一个从未访问的臂（$N_2=0$）对一条已访问 $N_1$ 次的臂，$\Delta B\approx c\,\pi^{1/\tau}\sqrt N$。记 $F(N)=c\pi^{1/\tau}\sqrt N$，它的含义是：探索项能“换来”多少步的价值差。

**2. 两项的尺度。**

- $\Delta B$：无量纲计数，随节点总访问量按 $\sqrt N$ 增长（$N\le15000$ 时 $\sqrt N\le122$）；
- $d_2-d_1$：整数步差，量子是 $1$，与 $N$ 无关，上界是证明视界 $T_{\max}$（约上百步）。

**3. 具体数值（重建参数 $c\in[1.25,1.82]$）。** 新臂的探索力：

- $N=100$、$\pi^{1/\tau}=0.1$ 时 $F\approx1.25\times0.1\times10\approx1.3$ 步；
- $N=15000$、$\pi^{1/\tau}=0.1$ 时 $F\approx15$ 步；$\pi^{1/\tau}=0.5$ 时 $F\approx77$ 步；$\pi^{1/\tau}=1$ 时 $F\approx153$ 步。

而一个 1 步差要被探索项翻盘，需要

$$
F(N)>1
\iff
N>\Big(\frac{1}{c\,\pi^{1/\tau}}\Big)^2
\quad(\pi^{1/\tau}=0.1,\ c=1.25\ \text{时约}\ 64\ \text{次访问}).
$$

**4. 结论——“量纲不匹配”的确切含义。** 交换率 $F(N)\propto\sqrt N$ 是变化的：

- 树上大多数节点访问次数少（搜索是稀疏的）：$F<1$，步差项支配选择，估计差 1 步就锁死，探索项形同虚设（这就是原句说的“Q 项支配”）；
- 根节点/大 $N$ 节点：$F$ 可达十几到上百步，探索项反而能压过几十步的价值差（过度探索）。

同一个常数 $c$ 在“小 $N$、中 $N$、大 $N$”下含义完全不同，所以原始步数直接相加没有一个可校准的探索温度。

**5. $\gamma$ 变换做了什么。** 令 $Q=\gamma^{d-1}$：

- **有界**：$Q\in(0,1]$，恰好是 PUCT 探索项最初设计所对应的价值尺度（AlphaZero 的胜负值就是 $[0,1]$），两项第一次落在同一量级；
- **每步代价恒定（相对）**：$Q(d+1)=\gamma Q(d)$，多一步总是损失价值的 $1-\gamma$（$\gamma=0.99$ 即 1%），于是步差在 Q 单位下是 $\Delta Q=Q(d)-Q(d+\Delta)=\gamma^{d-1}(1-\gamma^{\Delta})\le1$；
- 固定点分析下 $c$ 的含义变得干净：

$$
N_a\approx\frac{c\,\pi^{1/\tau}\sqrt N}{\mu-Q_a},
$$

即“相对价值赤字为 $\mu-Q_a$ 的臂分到多少访问”，$c$ 就是统一的探索温度，与步数量纲、与 $N$ 的具体大小解耦；
- 变换是单调的，不改变动作排序（§4.2 的贪婪最优性照常成立），改的只是尺度校准。

补充说明：论文本身只写了“把之前工作的 min-max 归一化换成 $Q=\gamma^{-V-1}$”，上面这套尺度比较是动机层面的推导（§4.3 里的有界性、兄弟独立、恒定相对步距才是可检验的严格性质）。
