# AlphaProof：MCTS + 强化学习的数学推导

参考：T. Hubert et al., "Olympiad-level formal mathematical reasoning with reinforcement learning", Nature (2025), doi:10.1038/s41586-025-09833-y。

本文是第 5 篇《MCTS + 强化学习：从贝尔曼方程到 AlphaZero》的定理证明版本：把 AlphaZero 式的搜索与训练换成 AlphaProof 的设定——状态是 Lean 战术状态，动作是战术字符串，回报是“剩余步数的负值”。全文按下面的线索展开，并始终关注同一个问题：**随着策略网络和价值网络参数的更新，性能是如何逐步提高的。**

1. 证明搜索的 MDP 与贝尔曼期望方程；
2. 贝尔曼最优方程（OR 节点取 max，AND 节点取 min）；
3. Q 变换与 PUCT 选择（含 AND 节点与渐进采样）；
4. 专家迭代：策略改进的数学形式，以及价值/策略网络如何被训练；
5. 主 RL 与测试时强化学习如何形成正反馈闭环。

---

## 1. Lean 证明环境作为 MDP

### 1.1 状态、动作、转移、回报

Lean 中一个数学命题是类型 $\Gamma \vdash P$，证明是该类型的一个项。AlphaProof 把 Lean 的战术模式（tactic mode）看作一个 MDP：

- 状态：规范化后的战术状态 $s=(\Gamma,\Delta)$，其中 $\Gamma$ 是排序、$\alpha$-重命名后的假设列表，$\Delta$ 是未关闭的目标列表；
- 动作：一条 Lean 战术字符串 $a$；
- 转移：$\mathrm{exec}(s,a)=s'$ 或无效。有效要求战术执行无错误、生成的证明项不含 `sorry`，且能通过内核检查；
- 奖励：每次成功应用的战术 $r_t=-1$。

回报与价值：

$$
G_t=\sum_{k\ge t}r_k,\qquad
V(s)=\mathbb{E}[G_t\mid s_t=s]=-d(s),
$$

其中 $d(s)$ 是从 $s$ 出发完成证明所需的期望战术数。**回报的语义是“剩余步数的负值”**，这一点会直接决定 Q 变换与价值网络的形状。

### 1.2 AND 节点：子目标分解与 min 回报

若某个战术把一个目标拆成 $N>1$ 个独立子目标，环境把状态拆成 $N$ 个子状态 $s^{(1)},\dots,s^{(N)}$（每个子状态只保留一个开放目标，其余暂用内部 `internalSorry` 关闭，最终证明仍由内核检查）。此时状态是 AND 节点，回报定义为

$$
G(s)=\min_{j\le N}G\big(s^{(j)}\big).
$$

而不是自然的求和。这个选择有一个直接的数学动机。

**均衡激励推导.** 设拆成 $k$ 个子目标，各分支长度为 $d_j$，总工作量 $W=\sum_j d_j$。由于分支独立求解，必然有

$$
\max_j d_j\ \ge\ \frac{W}{k},
$$

等号成立当且仅当所有 $d_j=W/k$。最大化 min 回报（等价于最小化 $\max_j d_j$）会推动 $d_j$ 趋向均衡；而求和目标 $\sum_j d_j=W$ 对不均衡不敏感，允许一条特别长的分支拖慢整体。这就是论文选择 min 回报的原因。

于是价值满足

$$
V(s_{\mathrm{AND}})=\min_j V\big(s^{(j)}\big).
$$

### 1.3 反证算子

为允许“证伪”，环境提供一个把目标取反的操作：对目标 $(\Gamma\vdash P)$ 撤销全部局部假设、应用私有公理

$$
\mathrm{internal\_true\_if\_false}:(\alpha\to\mathrm{False})\to\alpha,
$$

得到新目标

$$
\Big(\cdot\vdash\lnot\big(\forall\bar{x},\,P(\bar{x})\big)\Big).
$$

证伪与证明共享同一套搜索与训练机制；最终结论仍由内核独立验证（只允许 Lean 的三个标准公理）。

---

## 2. 贝尔曼期望方程（策略评估）

### 2.1 从回报递推推导

回报满足一步递推

$$
G_t=r_t+G_{t+1}.
$$

对任意固定策略 $\pi(a\mid s)$（在推断阶段，搜索过程本身充当策略），代入价值定义：

$$
V^\pi(s)
=\mathbb{E}_{\pi}[G_t\mid s_t=s]
=\sum_a\pi(a\mid s)\Big[r(s,a)+\sum_{s'}P(s'\mid s,a)V^\pi(s')\Big].
$$

证明环境中战术执行是确定性的，$P(s'\mid s,a)\in\{0,1\}$，因此退化为

$$
V^\pi(s)=\sum_a\pi(a\mid s)\big(-1+V^\pi(s')\big),
\qquad s'=\mathrm{exec}(s,a).
$$

这就是证明搜索的**贝尔曼期望方程**。它的意义是：价值是“少走一步的代价”加“下一步的价值”的自洽解。

### 2.2 算子形式与压缩性

定义贝尔曼期望算子

$$
(T_\pi V)(s)
=\sum_a\pi(a\mid s)\sum_{s'}P(s'\mid s,a)\big(r(s,a)+\gamma V(s')\big),
$$

其中 $\gamma\in(0,1)$ 是分析用的折扣因子（回报本身不折扣，理由见 4.1 节）。与第 5 篇同样的证明：对任意 $V,U$，

$$
\|T_\pi V-T_\pi U\|_\infty\le\gamma\|V-U\|_\infty,
$$

因为右边的期望是凸组合，且 $\sum_a\pi(a\mid s)\sum_{s'}P(s'\mid s,a)=1$。

由 Banach 不动点定理，$T_\pi$ 有唯一不动点 $V^\pi$，且值迭代

$$
V_{k+1}=T_\pi V_k
$$

满足

$$
\|V_k-V^\pi\|_\infty\le\gamma^k\|V_0-V^\pi\|_\infty.
$$

**为什么这有用**：策略评估是可收敛的过程。在证明搜索里，理论上可以用 $T_\pi$ 的后向归纳精确求值；实践上状态空间约有 $10^{100}$ 量级，只能靠“搜索采样到的状态 + 神经网络近似的叶子值”来做随机版的值迭代——这就是 MCTS 与价值网络存在的原因。

### 2.3 无折扣最短证明结构

证明搜索还有一个比压缩性更强的结构：对单目标确定性环境，最优值就是“最短证明步数的负值”。记 $d^*(s)$ 为从 $s$ 关闭全部目标的**最少战术数**，则

$$
d^*(s)=1+\min_a d^*\big(s'(a)\big),\qquad d^*(\text{目标全部关闭})=0,
$$

即

$$
V^*(s)=-d^*(s).
$$

在有限视界 $T_{\max}$（重建建议 $1024$）内，这是无折扣的动态规划；只要搜索树对这些状态是良基的，后向归纳在有限步内精确收敛。折扣因子 $\gamma$ 的作用不是定义价值，而是让 Q 变换（第 4 节）与神经网络的价值区间匹配，并给无穷/环状分支留出收缩余地。

---

## 3. 贝尔曼最优方程与策略改进

### 3.1 OR 节点的最优方程

由 $d^*$ 的递推直接得到最优方程

$$
V^*(s)=\max_a\Big\{-1+V^*\big(s'(a)\big)\Big\},
\qquad V^*(\text{关闭})=0.
$$

对应的最优贝尔曼算子

$$
(T^*V)(s)=\max_a\sum_{s'}P(s'\mid s,a)\big(r(s,a)+\gamma V(s')\big)
$$

同样是 $\gamma$-压缩，唯一不动点 $V^*$；值迭代误差按 $\gamma^k$ 衰减。

AND 节点上的最优方程就是 1.2 节的 min：

$$
V^*(s_{\mathrm{AND}})=\min_j V^*\big(s^{(j)}\big).
$$

直觉：**一个 AND 节点的价值由最难的分支决定**（木桶效应），所以搜索应该优先攻击最难子目标。

### 3.2 策略改进定理

**定理.** 设 $\pi$ 任意，令 $\pi'(s)=\arg\max_a Q^\pi(s,a)$。则

$$
V^{\pi'}(s)\ge V^\pi(s),\qquad\forall s.
$$

**证明.** 记 $V_0=V^\pi$，$V_{k+1}=T_{\pi'}V_k$。先看第一步：

$$
V_1(s)=-1+V^\pi\big(s'(\pi'(s))\big)=Q^\pi\big(s,\pi'(s)\big)\ge V^\pi(s),
$$

最后一步用了 $\pi'(s)$ 是贪婪动作。再由 $T_{\pi'}$ 的单调性归纳可得 $\{V_k\}$ 单调递增，而压缩性给出 $V_k\to V^{\pi'}$，于是 $V^{\pi'}\ge V^\pi$。∎

在证明搜索里，这个定理的含义是：**如果价值估计足够准，那么“按价值选战术”的搜索策略不会比当前策略差，而且每轮严格改进后策略会向最优策略靠近。**

### 3.3 与搜索的接口

AlphaProof 并不直接求解 $T^*$，而是用 MCTS 在“搜索可达的状态”上做近似：

- 搜索树的叶节点价值来自价值网络 $\widehat V(s_L)\approx V(s_L)$；
- 回传把叶值沿路径聚合，等价于对贝尔曼方程的迭代；
- 根节点的访问分布 $\hat\pi(a\mid s_0)=N(s_0,a)/N(s_0)$ 是搜索改进后的策略。

因此可以把搜索看作一个**近似策略迭代算子**：评估（回传）→ 改进（PUCT 选择），循环执行 $B$ 次模拟。每次模拟都让价值估计更准一点，也让访问分布更偏向高价值战术。

---

## 4. Q 变换：把“剩余步数”变成 (0,1] 的价值

### 4.1 为什么需要变换

搜索的原始价值是剩余步数 $d(s,a)$（或等价地 $V(s,a)=-d(s,a)$），量纲是“步数”。PUCT 要把价值和探索奖励相加，而奖励项是 $O(\sqrt N)$ 尺度的；直接把步数和奖励相加会因量纲不匹配而让 $Q$ 项支配选择。AlphaZero 曾用“除以当前子节点最大值/最小值”做 min-max 归一化，AlphaProof 改用一个显式变换：

$$
Q(s,a)=\gamma^{-V(s,a)-1}=\gamma^{\,d(s,a)-1},\qquad \gamma\in(0,1).
$$

由于 $d\ge 0$，有 $Q\in(0,1]$，且**关于价值单调递增**（关于步数单调递减）。论文原文：$Q(s,a)=\gamma^{-V(s,a)-1}$，其中 $V(s,a)$ 是聚合搜索价值，语义是“解决当前目标剩余步数的负值”。

### 4.2 贝尔曼递推在 Q 上的形式

单步关系 $d(s,a)=1+d(s')$ 与 OR 节点的聚合 $d(s')=\min_{a'}d(s',a')$ 给出

$$
Q(s,a)=\gamma^{\,d(s,a)-1}=\gamma^{\,d(s')}
=\gamma^{\,\min_{a'}d(s',a')}
=\gamma\max_{a'}Q(s',a').
$$

AND 节点由 $d(s_{\mathrm{AND}})=\max_j d(s^{(j)})$ 给出

$$
Q(s_{\mathrm{AND}})=\gamma^{\,\max_j d_j-1}=\min_j\gamma^{\,d_j-1}=\min_j Q\big(s^{(j)}\big).
$$

**贪婪最优性.** 在完全展开的树上，$\arg\max_a Q(s,a)$ 给出最短证明。证明（对 $d^*$ 归纳）：设 $d^*(s)=\ell$，则存在动作使 $d^*(s')=\ell-1$，且所有动作满足 $d^*(s')\ge\ell-1$；而 $d\mapsto\gamma^{d-1}$ 严格递减，故 $Q(s,a)$ 恰在 $d^*(s')=\ell-1$ 时最大，递归即得结论。

### 4.3 性质

- 范围：$d\le D_{\max}$ 时 $Q\in[\gamma^{D_{\max}-1},1]$；
- 兄弟独立：新增子节点不会改变已有边的 $Q$，不像 min-max 归一化会整体重标定；
- 移动行为：若 $\widehat V\to\widehat V+\kappa$，则 $Q\to\gamma^\kappa Q$，是保持排序的正比例缩放，只改变 $Q$ 与探索奖励的相对权重；
- 步距：$Q(d)-Q(d+1)=\gamma^{d-1}(1-\gamma)$，相对步距恒为 $1-\gamma$，绝对步距随 $d$ 指数缩小——**离目标越近，Q 越精确**，这与“分类价值头在近端分桶更密”的设计相配；
- 有效视界：$1/(1-\gamma)$，$\gamma=0.99$ 时约 $100$ 步（重建建议值；论文未在公开正文给出 $\gamma$）。

---

## 5. PUCT 选择：Sampled AlphaZero 的证明版

### 5.1 选择公式

AlphaProof 的选择规则（论文 Eq. 1）是

$$
a^\star=\arg\max_a\left[
Q(s,a)+c(s)\,\pi^{1/\tau}(a\mid s)\,
\frac{\sqrt{\sum_b N(s,b)}}{N(s,a)+1}
\right],
$$

其中

$$
c(s)=c_{\text{init}}+\log\left(\frac{N(s)+c_{\text{base}}+1}{c_{\text{base}}}\right)
$$

随节点访问数对数增长，补偿奖励项相对 $Q$ 的衰减。未访问的边用“父节点网络预测减去固定惩罚”初始化：

$$
V(s,a)=\widehat V_{\text{net}}(s)-c_{\text{pen}},
$$

即 $Q(s,a)=\gamma^{\,d_{\text{net}}(s)+c_{\text{pen}}-1}$：没试过的战术先按网络估计看，再惩罚一点。

先验温度有一个精确恒等式：若 $\pi(a\mid s)=\mathrm{softmax}(z)_a$，则

$$
\frac{\pi^{1/\tau}(a\mid s)}{\sum_b\pi^{1/\tau}(b\mid s)}
=\mathrm{softmax}\left(\frac{z}{\tau}\right)_a,
$$

即“对先验取 $1/\tau$ 次幂”就是“把 logits 除以 $\tau$”：$\tau>1$ 变平、$\tau<1$ 变尖。

### 5.2 扩展与回传

叶节点扩展时从策略网络采样 $K$ 条战术（重建建议 $K=64$），逐条在 Lean 中验证；无效丢弃；到达同一 Lean 状态（允许假设重命名与重排）的战术合并，保留代价

$$
\mathrm{cost}(a)=\mu\cdot|a|_{\text{tok}}+t_{\text{exec}}(a)
$$

最小的那条（论文：代价函数线性依赖字符串长度与执行时间）。叶值用网络估计 $V(s_L)=\widehat V_{\text{net}}(s_L)$。

回传沿路径更新

$$
N(s,a)\leftarrow N(s,a)+1,\qquad
d(s,a)\leftarrow\frac{(N-1)\,d(s,a)+d_{\text{child}}}{N};
$$

OR 节点的状态聚合取子节点最优（最短）：

$$
d(s)=\min_a d(s,a).
$$

### 5.3 AND 节点选择

AND 节点上的动作是“选择先攻哪个子目标”。选择公式把 $Q$ 换成 $1-Q$：

$$
j^\star=\arg\max_j\left[
\big(1-Q(s_{\mathrm{AND}},j)\big)
+c_{\mathrm{AND}}\,c(s_{\mathrm{AND}})\,\pi(j\mid s_{\mathrm{AND}})\,
\frac{\sqrt{\sum_b N(s_{\mathrm{AND}},b)}}{N(s_{\mathrm{AND}},j)+1}
\right],
$$

先验取均匀 $\pi(j)=1/k$。语义推导：

- $Q=\gamma^{d-1}$ 关于 $d$ 递减，所以 $1-Q$ 关于 $d$ 递增：**剩余步数最多（最难）的子目标得分最高**，搜索优先攻瓶颈，与 AND 节点的 max 备份一致；
- 已证明的子目标 $d_j=0$，此时 $Q_j=\gamma^{-1}>1$，故 $1-Q_j<0$，自动被降权，符合“只考虑未证明子目标”的规则。

### 5.4 渐进采样

为避免过早收窄候选集，若模拟路径上遇到节点满足

$$
n(s)\le C\,N(s)^{\alpha},
$$

则在该节点再采样 $K$ 条新战术加入边集（$n(s)$ 是此前采样过的战术数，$N(s)$ 是访问数）。直觉：访问少的节点靠采样多样性快速铺开（此时 $K$ 条采样很便宜），访问多的节点逐步加深。

### 5.5 单棵树，不承诺动作

一次证明尝试只维护一棵树，直到 `proved`、`disproved` 或预算耗尽。由于环境是确定性的单玩家场景，无需像博弈那样承诺动作或重开搜索，整棵树可以全局分配算力：

$$
\text{每次模拟成本}\ \approx\ O\big(K\,t_{\text{exec}}+K\,L\,d\big),
$$

其中 $L$ 是状态 token 数，$d$ 是模型维度。

---

## 6. 专家迭代：策略改进与“参数更新为何有效”

### 6.1 AlphaProof 的训练信号不是标准策略梯度

主 RL 的策略损失只对**成功搜索轨迹上的（状态，战术）对**做交叉熵：

$$
\mathcal{L}_{\text{pol}}(\theta)=-\log\pi_\theta(a\mid s),
$$

其中 $a$ 是成功路径上实际应用的战术。失败尝试不产生任何梯度（不引入“错的示范”）。这与标准策略梯度不同：没有裁剪、没有组相对优势、没有奖励折扣项，而是**专家迭代（expert iteration）**——把搜索（改进后的策略）当作监督者。

### 6.2 为什么交叉熵方向与策略梯度一致

把成功轨迹写成 $\tau=(s_0,a_0,s_1,a_1,\dots,s_T)$，其中 $s_T$ 无开放目标。策略复现整条轨迹的概率是

$$
P_\theta(\tau)=\prod_{t=0}^{T-1}\pi_\theta(a_t\mid s_t).
$$

交叉熵损失正是负对数似然：

$$
\mathcal{L}_{\text{pol}}(\theta)=-\log P_\theta(\tau).
$$

梯度下降增大 $\log P_\theta(\tau)$，即**增大“策略能复现这条已验证证明”的概率**。由于环境确定性，只要能复现 $\tau$ 就一定得到证明：

$$
P_\theta(\text{从 }s_0\text{ 证出})\ \ge\ P_\theta(\tau).
$$

另一方面，策略梯度中的每一项

$$
\nabla_\theta\log\pi_\theta(a_t\mid s_t)\,A(s_t,a_t)
$$

若把优势替换成“该动作位于成功路径上”的指示（视为正的常数权重），就退化为交叉熵的梯度。也就是说：

$$
\text{CE 到搜索成功动作}
\;\equiv\;
\text{以“成功=1、失败=0”为优势的策略梯度}.
$$

搜索始终由当前 $\theta$ 驱动，数据近似 on-policy，因此隐式信任域成立，不需要显式的裁剪。

### 6.3 价值网络的学习动力学

价值头用分类交叉熵拟合观测回报（分类桶）：

$$
\mathcal{L}_{\text{val}}(\theta)=-\log p_v\big(\mathrm{bin}(G(s_t))\mid s_t\big),
\qquad
\widehat V(s)=-\sum_b m_b\,p_v(b\mid s).
$$

对成功链 $s_0\to\dots\to s_T$，$G(s_t)=-(T-t)$；经过 AND 节点时取子目标的最小回报。交叉熵最小化的解是经验回报分布，从而

$$
\widehat V(s)\to\mathbb{E}[G(s)\mid s]=-d(s),
$$

即**价值网络逐步逼近当前搜索策略的真实剩余步数**。价值变准会带来两个直接后果：

- 叶节点的 $V(s_L)$ 更准，回传的 $d$ 更准；
- 未访问边的初始化（父节点预测减惩罚）更准，探索分配更合理。

### 6.4 单调改进的理想化命题

**命题.** 设环境确定性、价值估计精确、搜索在状态 $s$ 找到一条成功轨迹 $\tau$；把策略参数更新为 $\theta'$ 使 $P_{\theta'}(\tau)>P_\theta(\tau)$，并保持其他状态上的动作不变。则新策略在 $s$ 处的成功概率不降，且一旦沿 $\tau$ 前进就能在有限的 $T$ 步内完成证明。

这个命题是 6.2 的直接推论。现实中的偏差来自函数逼近误差、采样随机性与价值噪声，但**每次成功搜索都给出一个已验证的（状态，好动作）样本**，失败被剪掉，所以训练信号的“方向”始终指向搜索已证实可行的行为。

---

## 7. 网络架构：策略网络与价值网络

### 7.1 共享编码器

输入是状态的标准 pretty-print 字符串，用共享的代码/数学词表分词：

$$
x=\mathrm{tok}\big(\mathrm{pp}(s)\big)\in V^L,\qquad L\le L_{\max}.
$$

编码器输出隐表示与摘要向量：

$$
h=\mathrm{Enc}(x)\in\mathbb{R}^{L\times d},\qquad
\bar h=\mathrm{Enc}(x)[0].
$$

### 7.2 策略头（解码器）

自回归生成战术 token，交叉注意 $h$：

$$
\pi_\theta(a\mid s)=\prod_j p_\theta\big(a_j\mid a_{<j},h\big).
$$

推断时按温度采样 $K$ 条战术（而非 beam/greedy）——这对应搜索的“采样扩展”，保证候选集的多样性。候选的去重与验证由 Lean 环境完成。

### 7.3 价值头（分类分布）

$$
p_v(\cdot\mid\bar h)=\mathrm{softmax}\big(\mathrm{MLP}(\bar h)\big)\in\Delta^{B-1},
\qquad
\widehat V(s)=-\sum_b m_b\,p_v(b\mid s),
$$

其中 $m_b$ 是分类桶的中点。分类价值头有两个好处：

- 与 $Q$ 变换的“近端精度高、远端精度低”相匹配（对数间隔分桶）；
- 交叉熵比回归更稳定，能表达回报分布的多模态。

### 7.4 参数规模

每层参数量约 $12d^2$（注意力 $4d^2$ 加 FFN $8d^2$）。以 $d=2048$ 计：

- 编码器 32 层：约 $1.6\times10^9$；
- 解码器 20 层：约 $1.0\times10^9$；
- 词表嵌入（128K，与输出共享）：约 $0.26\times10^9$；
- 价值 MLP：约 $0.01\times10^9$。

合计约 $2.9\times10^9$，即论文所称的 3B 证明网络。

### 7.5 三阶段训练信号

1. 预训练：约 300B token 的代码与数学文本，编码器侧约 12T token、解码器侧约 3T token（约 50 个 epoch），下一 token 预测加 masked span 重建；
2. SFT：约 30 万条 Mathlib 的（状态，战术）对（约 10 TPU-days），策略头学 Lean 战术分布，价值头初始化回归剩余步数；
3. RL：主 RL 与 TTRL，见第 8、9 节。

---

## 8. 主 RL：训练闭环与性能提升

### 8.1 起始局面分布

与棋类“单一初始局面自对弈”不同，证明训练的起始状态来自问题数据集：

$$
\mathcal{D}_{\text{start}}
=\mathcal{D}_{\text{AutoForm}}\cup\mathcal{D}_{\text{human}},
\qquad
|\mathcal{D}_{\text{AutoForm}}|\approx8\times10^{7},
\quad
|\mathcal{D}_{\text{human}}|\approx3.5\times10^{3}.
$$

其中 $\mathcal{D}_{\text{AutoForm}}$ 由自动形式化流程从约 $10^6$ 条自然语言数学命题生成；人类形式化部分含 miniF2F-curriculum、人工形式化种子集与 PutnamBench 训练划分。

### 8.2 Matchmaker：问题选择与自适应预算

每个问题 $p$ 记录最近 $N$ 次尝试的结局（证明 / 证伪 / 超时）。设 $s_p$ 是最近 $N$ 次中被证明的比例，“有趣度”优先级定义为

$$
I(p)=\text{high}\iff
\Big[n_p=0\ \lor\ n_p<\mathrm{TC}\ \lor\
\big(n_p\ge\mathrm{TC}\ \land\ 0<s_p<1\big)\Big],
$$

即：从未尝试、尝试次数不够、或“有时能证出有时不能”的问题优先；连续证明过 $\mathrm{TC}$ 次（已掌握）或长期证不出的问题降权；已证伪的命题不再重试。每个问题的模拟预算按失败次数指数增长：

$$
B(p)=\min\Big(B_{\text{cap}},\ B_0\cdot m^{\,f_p}\Big),
\qquad
f_p=\#\{\text{最近 }N\text{ 次中的超时}\}.
$$

（重建建议：$N=25$、$\mathrm{TC}=5$、$B_0=500$、$m=2$、$B_{\text{cap}}=15000$。）

### 8.3 学习器

回放缓冲区只接收**已验证**的证明/证伪轨迹中的（状态，战术，回报）三元组，并按比例混合 Mathlib SFT 数据：

$$
\frac{|\mathcal{B}_{\text{SFT}}|}{|\mathcal{B}_{\text{self}}|}=\frac{1}{9}.
$$

损失是 6.1 与 6.3 的两项之和。10% 的 SFT 混合起正则作用，防止策略在稀疏状态空间上坍塌（对应第 5 篇的信任域思想）。

### 8.4 为什么参数更新会让性能逐步提高

把训练轮次记作 $k$，系统是一个迭代闭环：

$$
\pi_{\theta_k}
\;\to\;
\text{PUCT 搜索}
\;\to\;
\big(\text{成功轨迹},\ \text{回报}\hat d\big)
\;\to\;
\theta_{k+1}.
$$

- 策略头：每个成功轨迹给出若干“已验证的好动作”，交叉熵把复现该轨迹的概率 $P_{\theta}(\tau)$ 推高（6.2 节），所以下一轮搜索更容易沿可行路径展开；
- 价值头：$G(s)=-(T-t)$ 的精确样本让 $\widehat V$ 逼近真实剩余步数（6.3 节），$Q$ 变换后的排序更准，PUCT 的利用项与未访问边初始化都变好；
- 数据筛选：只有成功轨迹进入训练，失败不污染目标；已掌握的问题降权，算力转向“有时能证、有时不能”的问题——这正是策略提升空间最大的区域。

论文 Fig. 3 的测量与此一致：随 TPU-days 增加，证明比例上升、未决比例下降；并且**后期检查点在相同模拟预算下解出更多问题**（搜索效率随参数更新整体左移），即训练把“solve rate 关于模拟预算的曲线”整体推高。

### 8.5 计算规模

- 自动形式化：约 $10^5$ TPU-days；
- 主 RL：约 $8\times10^4$ TPU-days，约 $10^6$ 次参数更新；
- 单步代价近似 $C_{\text{step}}\approx6N_\theta\cdot\mathrm{batchTokens}$，但墙钟时间由 actor 的搜索成本主导（$B$ 次模拟 $\times$ $K$ 条战术执行）。

---

## 9. 测试时强化学习（TTRL）

### 9.1 动机

对一棵树搜到底也证不出的难题 $T$，AlphaProof 不再只加模拟预算，而是把整套 RL 机制**在测试时**作用于 $T$ 的一个定制课程。

### 9.2 变体生成与进化扩展

用 LLM 生成器 $\mathcal{G}$（少样本提示，示例集约 791 对）与程序化算子集 $\Sigma$ 生成变体，例如：

- 简化：$\big[\forall x\in A,\,P(x)\big]\mapsto\big[\forall x\in A'\subseteq A,\,P'(x)\big]$；
- 泛化：常量换成变量、弱化结论；
- 引理：提出 $C$ 并输出 $C$ 与 $C\to P$ 两个目标；
- 类比：$\lfloor\cdot\rfloor\to\lceil\cdot\rceil$、$S\to-S$、指标平移 $k\to k+1$、$\mathbb{R}\to\mathbb{Q}$；
- 改写：等式与互包含互换、$\forall$ 与有限集合条件互换；
- 证明步骤：给出已知骨架的中间目标。

扩展循环（$N_{\text{evo}}$ 轮，论文取值 $15$）：

$$
V_0=\mathrm{Valid}\big(\mathcal{G}(\Omega,T)\big),\qquad
V_{i+1}=V_i\cup\mathrm{Valid}\Big(\mathcal{G}\big(\mathrm{top}_k\ V_i\ \text{by}\ \mathrm{sim}(T,\cdot)\big)\Big),
$$

最终得到数十万到上百万条语法合法的 Lean 变体。难度代理可以取

$$
\mathrm{diff}(v)=1-\mathrm{sim}(T,v)
\quad\text{或}\quad
\mathrm{diff}_{\text{emp}}(v)=\frac{1}{1+\mathrm{solveRate}(v)}.
$$

论文消融显示：课程越“难而不至于无解”，对目标 $T$ 的最终提升越大，即存在课程难度最优区间，而不是越简单越好。

### 9.3 定向 RL

从主 RL 的通用模型出发，对目标 $T$ 做定向微调，起始分布为

$$
\mathcal{D}_T=\{T\}\cup V_T,
$$

其余机制与主 RL 完全相同（同样的 matchmaker、90/10 混合、同样两种损失）。一旦 $T$ 被证明，matchmaker 停止分配 $T$ 及其变体；多个目标可以共享一个 matchmaker 并行 TTRL。

### 9.4 为什么 TTRL 能突破搜索饱和

- 变体与 $T$ 共享数学骨架（引理、界、不变量），所以从成功变体得到的参数更新**直接改善 $T$ 附近的策略**；
- min 回报价值让搜索预算自动集中在最难分支上，而这些分支正是 $T$ 的关键障碍；
- 定向 RL 把主 RL 的“通用策略改进”变成“围绕 $T$ 的局部策略改进”：每轮成功变体都在为 $T$ 的证明路径铺路。

论文测量：在 formal-imo 与 PutnamBench-test 上，TTRL 相对 500 TPU-days 的纯搜索带来约 $+15$ 个百分点的绝对提升（在每题 4000 次模拟下评估）；总体成绩（Table 1）：2 TPU-min 推理时 miniF2F-test $96.3\%$、formal-imo $33.2\%$、PutnamBench-test $27.9\%$；TTRL 500 TPU-days 后分别达到 $99.6\%$、$58.3\%$、$56.1\%$。

---

## 10. 总结：与 AlphaZero 版的对应与差异

1. **证明 MDP 与贝尔曼期望方程**
   - 状态是 Lean 战术状态，动作是战术字符串，奖励恒为 $-1$，价值是“剩余步数的负值”；
   - 固定策略的价值满足贝尔曼期望方程，$\gamma$-压缩，值迭代误差按 $\gamma^k$ 衰减；
   - 单目标确定性环境下最优价值就是最短证明步数。
2. **贝尔曼最优方程**
   - OR 节点：$V^*(s)=\max_a\{-1+V^*(s')\}$；
   - AND 节点：$V^*(s_{\text{AND}})=\min_j V^*(s^{(j)})$，min 回报带来“子目标难度均衡”的激励；
   - 策略改进定理保证贪婪改进步步不劣。
3. **Q 变换与 PUCT**
   - $Q(s,a)=\gamma^{-V(s,a)-1}=\gamma^{d-1}\in(0,1]$，把步数变成与探索奖励同量纲的价值；
   - PUCT 选择 = 利用 $Q$ + 先验 $\pi^{1/\tau}$ 加权的计数值探索；
   - AND 节点用 $1-Q$ 优先攻瓶颈，已解决子目标自动降权；
   - 渐进采样与单树搜索保证多样性与全局算力分配。
4. **专家迭代与网络**
   - 策略头：对成功搜索轨迹做交叉熵，等价于“成功=1”的优势加权策略梯度，参数更新直接提高复现已验证证明的概率；
   - 价值头：分类交叉熵拟合 $G=-(T-t)$，逼近真实剩余步数，使 Q 排序与探索分配越来越准；
   - 3B 编码器-解码器，采样 $K$ 条战术，三阶段训练（预训练 / SFT / RL）。
5. **闭环提升**
   - 主 RL：更好策略 → 搜索找到更多证明 → 更多已验证样本 → 策略与价值更准；
   - TTRL：搜索饱和后，用目标 $T$ 的变体课程做定向 RL，把通用改进变成对 $T$ 的局部改进；
   - 每一步参数更新都同时改善“评估精度”和“改进方向”，这就是性能随训练逐步提高的数学结构。

与第 5 篇（AlphaZero 版）的核心差异一览：

- 奖励为 $-1$ 且回报不折扣，$\gamma$ 只出现在 Q 变换里；
- 引入 AND 节点与 min 回报、反证算子；
- 搜索不承诺动作、单树到底（确定性单玩家环境）；
- 价值头是分类分布，预测剩余步数而非胜负概率；
- 训练是专家迭代（成功轨迹交叉熵 + 分类价值损失），而非带优势的策略梯度；
- 起始分布是问题课程（含自动形式化生成的约 8000 万命题），而非单一初始局面；
- 测试时除了加搜索预算，还可整体重跑 RL（TTRL）。
