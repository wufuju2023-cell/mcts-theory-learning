6-alpha-proof-mcts-rl.md:191（§3.2 策略改进定理证明）。这个等式来自 $Q^\pi$ 的一步展开，两边其实是同一个“备份”：

1. 为什么 $Q^\pi(s,\pi'(s))=-1+V^\pi(s'(\pi'(s)))$。 动作价值是对“先执行 $a$、之后按 $\pi$ 走”的回报取期望：

$$
Q^\pi(s,a)=\mathbb{E}_\pi\big[G_t\mid s_t=s,a_t=a\big]
=\mathbb{E}_\pi\big[r_t+V^\pi(s_{t+1})\mid s_t=s,a_t=a\big].
$$

在证明 MDP 里：奖励无随机性、每应用一个战术恰为 $-1$（$r_t=-1$）；战术执行确定性，后继状态 $s'(a)=\mathrm{exec}(s,a)$；其后继续遵循 $\pi$，期望回报就是 $V^\pi(s'(a))$。期望因此退化为确定式：

$$
Q^\pi(s,a)=-1+V^\pi\big(s'(a)\big),
$$

取 $a=\pi'(s)$ 即得等式右端。
2. 左端 $V_1(s)$ 的来源。 $V_1=T_{\pi'}V^\pi$，而 $\pi'(s)$ 是确定性贪婪动作，所以对 $a$ 的求和坍缩成一项：

$$
V_1(s)=\sum_a\pi'(a\mid s)\big(-1+V^\pi(s'(a))\big)=-1+V^\pi\big(s'(\pi'(s))\big).
$$

所以 $V_1(s)$（用旧价值 $V^\pi$ 对 $\pi'$ 走一步的评估）与 $Q^\pi(s,\pi'(s))$（贪婪动作的 Q 值）完全相等。
3. 附带的 $\ge$。 那一步是贪婪性：$Q^\pi(s,\pi'(s))=\max_aQ^\pi(s,a)\ge\sum_a\pi(a\mid s)Q^\pi(s,a)=V^\pi(s)$，即“$\pi'$ 在 $s$ 处的优势非负”。
一个小提醒：§2.2 定义的算子 $T_\pi$ 带折扣 $\gamma$，但 §3 这条推导用的是证明回报的精确约定（无折扣、$r_t=-1$，与 §2.3、§3.1 的最短证明结构一致）。若严格按带 $\gamma$ 的算子写，这条链应为 $-1+\gamma V^\pi(s'(\pi'(s)))=Q^\pi_\gamma(s,\pi'(s))$。
